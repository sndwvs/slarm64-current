--- a/x11.SlackBuild	2017-10-01 23:32:56.364747191 +0300
+++ b/x11.SlackBuild	2017-10-01 23:37:37.036747191 +0300
@@ -76,10 +76,21 @@
 
 if [ "$ARCH" = "x86_64" ]; then
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  LIBDIRSUFFIX="64"
 else
   LIBDIRSUFFIX=""
 fi
 
+# add build aarch64
+xzcat arch.use.flags.patch.xz | patch -p1 --verbose || exit 1
+xzcat package-blacklist.patch.xz | patch -p1 --verbose || exit 1
+
+# fix build arch
+sed  -e 's/\(-slackware\)\(-linux.*\s\)/-unknown\2/g' \
+     -e 's/\(-slackware\)\(-linux$\)/-unknown\2/g' \
+     -i configure/*
+
 # Set up a few useful functions:
 
 fix_perms() {
