--- a/etc.SlackBuild	2018-04-30 22:21:28.240046514 +0100
+++ b/etc.SlackBuild	2018-04-30 22:22:41.144046549 +0100
@@ -55,9 +55,9 @@
 # Fix for ARCH:
 sed -i -e "s#/i586-slackware-linux#/${ARCH}-slackware-linux#" etc/ld.so.conf.new
 # Fix paths if lib64 is needed:
-if [ "$ARCH" = "x86_64" ]; then
-  sed -i -e "s#/lib#/lib64#" etc/ld.so.conf.new
-fi
+case "$ARCH" in
+  x86_64|aarch64) sed -i -e "s#/lib#/lib64#" etc/ld.so.conf.new ;;
+esac
 zcat $CWD/nsswitch.conf.gz > $PKG/etc/nsswitch.conf.new
 
 mkdir -p $PKG/install
