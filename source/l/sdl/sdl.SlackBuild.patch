--- a/sdl.SlackBuild	2020-02-13 16:45:11.121723443 +0000
+++ b/sdl.SlackBuild	2020-02-13 16:46:42.099012586 +0000
@@ -61,6 +61,9 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -97,7 +100,9 @@
   --disable-esd \
   --enable-shared=yes \
   --enable-static=no \
-  --disable-x11-shared || exit 1
+  --disable-x11-shared \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 
@@ -139,7 +144,9 @@
   --enable-static=no \
   --enable-jpg-shared=no \
   --enable-png-shared=no \
-  --enable-tif-shared=no || exit 1
+  --enable-tif-shared=no \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 make install DESTDIR=$PKG || exit 1
@@ -166,7 +173,9 @@
   --libdir=/usr/local/lib${LIBDIRSUFFIX} \
   --with-pic \
   --enable-shared=no \
-  --enable-static=yes || exit 1
+  --enable-static=yes \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
   make $NUMJOBS || make || exit 1
   make install || exit 1
 ) || exit 1
@@ -185,7 +194,9 @@
   --mandir=/usr/man \
   --enable-music-mod \
   --enable-shared=yes \
-  --enable-static=no || exit 1
+  --enable-static=no \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 make install DESTDIR=$PKG || exit 1
@@ -216,7 +227,9 @@
   --libdir=/usr/lib${LIBDIRSUFFIX} \
   --mandir=/usr/man \
   --enable-shared=yes \
-  --enable-static=no || exit 1
+  --enable-static=no \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 make install DESTDIR=$PKG || exit 1
@@ -247,7 +260,9 @@
   --libdir=/usr/lib${LIBDIRSUFFIX} \
   --mandir=/usr/man \
   --enable-shared=yes \
-  --enable-static=no || exit 1
+  --enable-static=no \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 make install DESTDIR=$PKG || exit 1
@@ -272,7 +287,9 @@
   --sysconfdir=/etc \
   --mandir=/usr/man \
   --enable-shared=yes \
-  --enable-static=no || exit 1
+  --enable-static=no \
+  --host=$ARCH-slackware-linux \
+  --build=$ARCH-slackware-linux || exit 1
 
 make $NUMJOBS || make || exit 1
 make install DESTDIR=$PKG || exit 1
