--- a/libunwind.SlackBuild	2021-12-13 11:18:58.489453088 +0200
+++ b/libunwind.SlackBuild	2021-12-13 11:23:02.137355718 +0200
@@ -59,6 +59,12 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC -fcommon"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2 -fPIC -fcommon"
+  LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "riscv64" ]; then
+  SLKCFLAGS="-O2 -fPIC -fcommon"
+  LIBDIRSUFFIX="64"
 elif [ "$ARCH" = "armv7hl" ]; then
   SLKCFLAGS="-O3 -march=armv7-a -mfpu=vfpv3-d16 -fcommon"
   LIBDIRSUFFIX=""
@@ -85,6 +91,11 @@
   \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
   -exec chmod 644 {} \+
 
+if [ "$ARCH" = "riscv64" ]; then
+    VERSION="1.6.2"
+    xzcat $CWD/libunwind-1.6.2.patch.xz | patch -p1 || exit 1
+fi
+
 # Configure:
 CFLAGS="$SLKCFLAGS" \
 ./configure \
