--- a/cpufrequtils.SlackBuild	2018-02-21 18:13:23.485196874 +0000
+++ b/cpufrequtils.SlackBuild	2018-02-21 18:13:27.620196874 +0000
@@ -50,6 +50,9 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -68,6 +71,8 @@
   \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
   -exec chmod 644 {} \;
 
+xzcat $CWD/0001-Only-x86-has-cpuid-instruction.patch.xz | patch -p1 --verbose || exit 1
+
 # Build and install:
 make libdir=/usr/lib${LIBDIRSUFFIX}
 make install libdir=/usr/lib${LIBDIRSUFFIX} DESTDIR=$PKG
