--- a/patch/xorg-server.patch	2019-11-23 21:26:04.535652811 +0200
+++ b/patch/xorg-server.patch	2020-09-05 03:52:04.784698624 +0300
@@ -22,3 +22,9 @@
 # Reference: https://bugs.freedesktop.org/show_bug.cgi?id=94844
 # Added here 2018/7.
 zcat $CWD/patch/xorg-server/0001-xfree86-use-modesetting-driver-by-default-on-GeForce.patch.gz | patch -p1 --verbose || { touch ${SLACK_X_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
+
+# 28-Aug-2020:
+# Introduced in xorg-server 1.20.9 - was fine in .8
+# This patch comes from Red Hat, and fixes an old bug on ARM - but until this
+# point, I'd never seen it!
+zcat $CWD/patch/xorg-server/stop-segfaulting.diff.gz | patch -p1 --verbose || { touch ${SLACK_X_BUILD_DIR}/${PKGNAME}.failed ; continue ; }
