--- a/seyon.SlackBuild	2021-02-13 05:32:09.757944644 +0000
+++ b/seyon.SlackBuild	2021-03-27 12:39:34.767007664 +0000
@@ -60,6 +60,12 @@
 elif [ "$ARCH" = "s390" ]; then
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
+  LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "riscv64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
+  LIBDIRSUFFIX="64"
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
@@ -94,6 +100,8 @@
 xmkmf
 # Add -fcommon to CFLAGS:
 sed -i -e 's/-fno-strict-aliasing/-fno-strict-aliasing -fcommon/' Makefile
+# fix riscv64
+[[ $ARCH == 'riscv64' ]] && sed -i -e "s/LinuxMachineDefines/$SLKCFLAGS -fcommon/g" Makefile
 make || exit 1
 
 mkdir -p $PKG/usr/lib${LIBDIRSUFFIX}/X11
