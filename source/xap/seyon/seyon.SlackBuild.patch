--- a/seyon.SlackBuild	2017-10-04 22:26:09.221506609 +0300
+++ b/seyon.SlackBuild	2017-10-04 22:29:23.024506609 +0300
@@ -57,6 +57,9 @@
 elif [ "$ARCH" = "armv7hl" ]; then
   SLKCFLAGS="-O3 -march=armv7-a -mfpu=vfpv3-d16"
   LIBDIRSUFFIX=""
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -79,7 +82,10 @@
   \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
   -exec chmod 644 {} \;
 
-zcat $CWD/seyon_2.20c-16.diff.gz | patch -p1 --verbose || exit 1
+#zcat $CWD/seyon_2.20c-16.diff.gz | patch -p1 --verbose || exit 1
+xzcat $CWD/01-debian-diff-to-be-split-some-day.patch.xz | patch -p1 --verbose || exit 1
+xzcat $CWD/02-rename-getline-to-seyon-getline.patch.xz | patch -p1 --verbose || exit 1
+xzcat $CWD/03-reproducible-build.patch.xz | patch -p1 --verbose || exit 1
 
 chmod 755 makever.sh
 xmkmf
@@ -135,4 +141,3 @@
 
 cd $PKG
 /sbin/makepkg -l y -c n $TMP/seyon-$VERSION-$ARCH-$BUILD.txz
-
