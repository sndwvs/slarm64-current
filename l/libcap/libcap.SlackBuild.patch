--- a/libcap.SlackBuild	2018-04-25 20:12:35.186200023 +0300
+++ b/libcap.SlackBuild	2018-04-25 20:21:33.597813142 +0300
@@ -63,6 +63,9 @@
 elif [ "$ARCH" = "armel" ]; then
   SLKCFLAGS="-O2 -march=armv4t"
   LIBDIRSUFFIX=""
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 fi
 
 TMP=${TMP:-/tmp}
@@ -91,8 +94,8 @@
 sed 's@^BUILD_GPERF@#\0@' -i Make.Rules
 
 
-make DYNAMIC=yes $NUMJOBS || make DYNAMIC=yes || exit 1
-make install FAKEROOT=$PKG man_prefix=/usr || exit 1
+make DYNAMIC=yes $NUMJOBS lib=lib${LIBDIRSUFFIX} || make DYNAMIC=yes lib=lib${LIBDIRSUFFIX} || exit 1
+make install FAKEROOT=$PKG man_prefix=/usr lib=/lib${LIBDIRSUFFIX} RAISE_SETFCAP=no || exit 1
 chmod 755 $PKG/lib${LIBDIRSUFFIX}/libcap.so*
 
 find $PKG | xargs file | grep -e "executable" -e "shared object" | grep ELF \
