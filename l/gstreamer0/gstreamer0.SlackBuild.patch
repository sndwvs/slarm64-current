--- a/gstreamer0.SlackBuild	2017-09-23 21:32:43.340800443 +0300
+++ b/gstreamer0.SlackBuild	2017-09-23 21:34:20.451800443 +0300
@@ -51,6 +51,9 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -74,6 +77,12 @@
  \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
  -exec chmod 644 {} \;
 
+# Apply patches:
+# http://lists.linuxfromscratch.org/pipermail/blfs-dev/2013-August/025629.html
+sed -i  -e '/YYLEX_PARAM/d'                                       \
+        -e '/parse-param.*scanner/i %lex-param { void *scanner }' \
+        gst/parse/grammar.y
+
 CFLAGS="$SLKCFLAGS" \
 CXXFLAGS="$SLKCFLAGS" \
 ./configure \
