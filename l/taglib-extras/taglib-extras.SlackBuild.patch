--- a/taglib-extras.SlackBuild	2017-09-30 13:32:27.953336192 +0300
+++ b/taglib-extras.SlackBuild	2017-09-30 13:33:38.651336192 +0300
@@ -45,6 +45,9 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -67,6 +70,8 @@
   \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
   -exec chmod 644 {} \;
 
+xzcat $CWD/taglib-1.10.patch.xz | patch -p1 --verbose || exit 1
+
 mkdir -p build
 cd build
   cmake \
@@ -95,4 +100,3 @@
 
 cd $PKG
 /sbin/makepkg -l y -c n $TMP/taglib-extras-$VERSION-$ARCH-$BUILD.txz
-
