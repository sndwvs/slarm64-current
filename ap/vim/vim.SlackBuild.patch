--- a/vim.SlackBuild	2017-09-20 22:05:35.260174937 +0300
+++ b/vim.SlackBuild	2017-09-20 22:06:07.676174937 +0300
@@ -51,13 +51,16 @@
   esac
 fi
 
-NUMJOBS=${NUMJOBS:-" -j7 "}
+NUMJOBS=${NUMJOBS:-" -j5 "}
 
 PYVER=$(python -V 2>&1 | cut -f 2 -d' ' | cut -f 1-2 -d.)
 
 if [ "$ARCH" = "x86_64" ]; then
   export SLKCFLAGS="-O2 -fPIC"
   export LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  export SLKCFLAGS="-O2"
+  export LIBDIRSUFFIX="64"
 else
   export SLKCFLAGS="-O2"
   export LIBDIRSUFFIX=""
