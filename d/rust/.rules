
rules() {
    export LOCAL_BOOTSTRAP="no"
    pushd ${_WORK_DIR} 2>&1>/dev/null
    ( xzcat rust.url.patch.xz | patch -p1 ) || return 1
    ( grep aarch64 rust.url | sh ) || return 1
    popd 2>&1>/dev/null
}

rules
