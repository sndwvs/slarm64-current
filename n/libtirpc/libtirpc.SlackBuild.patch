--- a/libtirpc.SlackBuild	2017-09-30 13:49:10.343154086 +0300
+++ b/libtirpc.SlackBuild	2017-09-30 13:52:17.619852556 +0300
@@ -68,6 +68,9 @@
 elif [ "$ARCH" = "armv7hl" ]; then
   SLKCFLAGS="-O3 -march=armv7-a -mfpu=vfpv3-d16"
   LIBDIRSUFFIX=""
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -92,6 +95,12 @@
   \( -perm 666 -o -perm 664 -o -perm 600 -o -perm 444 -o -perm 440 -o -perm 400 \) \
   -exec chmod 644 {} \;
 
+# Fix a build issue with glibc-2.26:
+sed '/stdlib.h/a#include <stdint.h>' -i src/xdr_sizeof.c
+
+# Fix a symbol name needed by NIS libraries:
+sed '/key_secret_is/s/secret/secretkey/' -i src/libtirpc.map
+
 # Set proper GSS option:
 if [ "$WITH_GSS" = "NO" ]; then
   GSSAPI="--disable-gssapi"
