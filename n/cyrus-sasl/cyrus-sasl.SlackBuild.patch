--- a/cyrus-sasl.SlackBuild	2017-11-30 22:28:31.646167435 +0200
+++ b/cyrus-sasl.SlackBuild	2017-11-30 22:29:58.461167435 +0200
@@ -47,6 +47,9 @@
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -fPIC"
   LIBDIRSUFFIX="64"
+elif [ "$ARCH" = "aarch64" ]; then
+  SLKCFLAGS="-O2"
+  LIBDIRSUFFIX="64"
 else
   SLKCFLAGS="-O2"
   LIBDIRSUFFIX=""
@@ -82,7 +85,8 @@
   --with-saslauthd \
   --with-gdbm \
   --with-devrandom=/dev/urandom \
-  --with-dblib=gdbm || exit 1
+  --with-dblib=gdbm \
+  --build=$ARCH-slackware-linux || exit
 
 # How stupid that I need to specify 'sasldir' again for 'make' or else you get
 # the warning "Plugins are being installed into /usr/lib/sasl2, but the library
